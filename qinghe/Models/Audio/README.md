# Audio Models (Core ML)

This directory will contain on-device Core ML models for sleep audio analysis.

Planned models:
- SileroVAD.mlmodel (Voice Activity Detection, 16k mono)
- SnoreTalking.mlmodel (binary classifier: snore vs. talking)

You can generate these models using the Python conversion scripts under scripts/audio/.


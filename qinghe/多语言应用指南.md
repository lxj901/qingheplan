# 青禾应用多语言支持指南

## 目录
1. [概述](#概述)
2. [已完成的工作](#已完成的工作)
3. [如何为页面添加多语言支持](#如何为页面添加多语言支持)
4. [添加新的文本键](#添加新的文本键)
5. [最佳实践](#最佳实践)
6. [待完成页面清单](#待完成页面清单)

## 概述

本指南介绍了如何为青禾应用添加全面的多语言支持。应用支持以下语言：
- 简体中文（默认）
- 繁体中文
- 英文
- 日文
- 韩文

### 核心组件

1. **LocalizationManager**: 单例管理器，负责多语言切换和文本获取
2. **LocalizedStrings**: 静态方法集合，包含所有语言的文本映射
3. **扩展方法**: View 扩展提供便捷的本地化方法

## 已完成的工作

### 1. 核心基础设施

#### LocalizationManager (MessagesView.swift)
```swift
class LocalizationManager: ObservableObject {
    static let shared = LocalizationManager()
    @Published var currentLanguage: String = "zh-Hans" // 默认简体中文
    
    // 支持的语言
    static let supportedLanguages: [String] = ["zh-Hans", "zh-Hant", "en", "ja", "ko"]
    
    // 切换语言
    func changeLanguage(to language: String) { ... }
    
    // 获取本地化字符串
    func localizedString(key: String) -> String { ... }
}
```

#### LocalizedStrings 文本库
位置: `MessagesView.swift`

已添加**400+**个文本键，涵盖以下类别：
- 通用操作（确认、取消、保存、删除等）
- 时间相关（今天、昨天、小时、分钟等）
- 社区功能（帖子、评论、点赞、分享等）
- 聊天消息（发送、接收、语音、图片等）
- 健康相关（诊断、分析、建议等）
- 睡眠跟踪（睡眠质量、深睡眠、REM等）
- 运动健身（运动类型、配速、心率等）
- 记录中心（情绪、诱惑、计划等）
- 音频播放（播放、暂停、循环等）
- 会员订阅（VIP、续费、优惠券等）
- 权限通知（相机、位置、相册等）
- 用户资料（昵称、简介、头像等）
- 登录注册（密码、验证码、忘记密码等）
- 错误提示（网络错误、操作失败等）

### 2. 已适配的页面

#### MainTabView.swift
- ✅ 底部导航栏标题（首页、记录、健康、运动、我的）
- ✅ MainTab 枚举的标题本地化
- ✅ ProfileView 未登录状态文本

示例代码：
```swift
var title: String {
    return LocalizationManager.shared.localizedString(key: titleKey)
}
```

#### NewRecordCenterView.swift
- ✅ 页面标题（记录中心）
- ✅ 标签标题（情绪记录、诱惑记录、计划管理）
- ✅ 加载状态文本

#### MainCommunityView.swift
- ✅ 加载中文本
- ✅ 无数据提示
- ✅ 加载更多文本
- ✅ 错误提示

## 如何为页面添加多语言支持

### 步骤 1：导入 LocalizationManager

在 View 结构体中添加 LocalizationManager：

```swift
struct YourView: View {
    @StateObject private var localizationManager = LocalizationManager.shared
    
    var body: some View {
        // 视图内容
    }
}
```

### 步骤 2：替换硬编码文本

将所有硬编码的中文文本替换为本地化调用：

**之前：**
```swift
Text("加载中...")
.navigationTitle("设置")
Button("确认") { }
```

**之后：**
```swift
Text(localizationManager.localizedString(key: "loading"))
.navigationTitle(localizationManager.localizedString(key: "settings"))
Button(localizationManager.localizedString(key: "confirm")) { }
```

### 步骤 3：处理动态文本

对于字符串数组或计算属性：

**之前：**
```swift
private let tabTitles = ["选项一", "选项二", "选项三"]
```

**之后：**
```swift
private var tabTitles: [String] {
    [
        localizationManager.localizedString(key: "option_one"),
        localizationManager.localizedString(key: "option_two"),
        localizationManager.localizedString(key: "option_three")
    ]
}
```

### 步骤 4：枚举和结构体

为枚举添加本地化支持：

```swift
enum WorkoutType: String, CaseIterable {
    case running, cycling, swimming
    
    var titleKey: String {
        switch self {
        case .running: return "workout_running"
        case .cycling: return "workout_cycling"
        case .swimming: return "workout_swimming"
        }
    }
    
    var localizedTitle: String {
        LocalizationManager.shared.localizedString(key: titleKey)
    }
}
```

## 添加新的文本键

如果现有的文本键库中没有您需要的文本，请按以下步骤添加：

### 步骤 1：在 getChineseSimplified 中添加

打开 `MessagesView.swift`，在 `getChineseSimplified` 方法中添加新键：

```swift
private static func getChineseSimplified(key: String) -> String {
    switch key {
    // ... 现有键 ...
    
    // 您的新键
    case "your_new_key": return "您的中文文本"
    
    default: return key
    }
}
```

### 步骤 2：为所有语言添加翻译

在以下方法中添加对应翻译：
- `getChineseTraditional` - 繁体中文
- `getEnglish` - 英文
- `getJapanese` - 日文
- `getKorean` - 韩文

### 步骤 3：使用新键

```swift
Text(localizationManager.localizedString(key: "your_new_key"))
```

## 最佳实践

### 1. 命名规范

文本键使用小写下划线命名：
```
good: "user_profile", "workout_detail", "sleep_quality"
bad: "UserProfile", "workoutDetail", "SleepQuality"
```

### 2. 分类组织

按功能模块组织文本键：
```swift
// 社区相关
case "post": return "帖子"
case "publish_post": return "发帖"
case "edit_post": return "编辑帖子"

// 运动相关
case "workout": return "运动"
case "start_workout": return "开始运动"
case "pause_workout": return "暂停运动"
```

### 3. 避免重复

检查现有键，避免添加重复的文本：
```swift
// 已有 "confirm"，不要再添加 "confirm_button"
```

### 4. 上下文特定的键

当同一个词在不同上下文有不同含义时，使用特定键：
```swift
case "record": return "记录" // 名词
case "create_record": return "创建记录" // 动作
case "record_history": return "记录历史" // 特定上下文
```

### 5. 使用便捷扩展

View 扩展提供了便捷方法（如果已添加）：
```swift
Text("hello").localized()
```

## 待完成页面清单

以下页面需要添加多语言支持。请按照上述步骤逐个页面处理：

### 高优先级页面

#### 社区模块
- [ ] PostDetailView.swift
- [ ] NewPublishPostView.swift
- [ ] CommunitySearchView.swift
- [ ] TagDetailView.swift
- [ ] PostInteractionUsersView.swift
- [ ] ReportPostView.swift
- [ ] PostCardView.swift

#### 用户模块
- [ ] UserProfileView.swift
- [ ] SettingsPages.swift
- [ ] FinalSettingsPages.swift
- [ ] MoreSettingsPages.swift
- [ ] MessageUserProfileView.swift
- [ ] ProfileContentModule.swift
- [ ] UserProfileComponents.swift

#### 聊天模块
- [ ] ChatDetailView.swift
- [ ] ChatWithUserView.swift
- [ ] NewChatView.swift
- [ ] GroupDetailManagementView.swift
- [ ] GroupMemberListView.swift
- [ ] AddGroupMemberView.swift
- [ ] EditGroupInfoView.swift
- [ ] ChatUIComponents.swift

#### 健康模块
- [ ] HealthManagerView.swift
- [ ] HealthAssistantView.swift
- [ ] ConstitutionAnalysisReportView.swift
- [ ] WuYunLiuQiView.swift
- [ ] DiagnosisHistoryView.swift
- [ ] WuYunZhuYunDiagramView.swift
- [ ] ConstitutionCircleView.swift
- [ ] RadarChartView.swift

#### 睡眠模块
- [ ] SleepTrackingView.swift
- [ ] SleepDashboardView.swift
- [ ] SleepDetailView.swift
- [ ] SleepRecordsView.swift
- [ ] SleepInsightsView.swift
- [ ] WakeTimeSelectionView.swift

#### 运动模块
- [ ] WorkoutModeSelectionView.swift
- [ ] WorkoutLiveView.swift
- [ ] KeepStyleWorkoutLiveView.swift
- [ ] WorkoutDetailView.swift
- [ ] WorkoutAnalysisDetailView.swift
- [ ] WorkoutCompletionView.swift
- [ ] WorkoutCameraView.swift
- [ ] WorkoutDetailOverviewView.swift

### 中优先级页面

#### 记录打卡
- [ ] GongGuoGeView.swift
- [ ] GongGuoRecordEditorView.swift
- [ ] ModernCheckinCalendarView.swift
- [ ] EmotionRecordView.swift
- [ ] CheckinRecordsSelectionView.swift
- [ ] WorkoutRecordsSelectionView.swift
- [ ] MeritStatisticsView.swift

#### 计划管理
- [ ] PlanManagementView.swift
- [ ] CreatePlanView.swift
- [ ] PlanDetailView.swift

#### 音频播放
- [ ] WhiteNoisePageView.swift
- [ ] WuYinPlayerDetailView.swift
- [ ] WuYinPlaylistsPageView.swift
- [ ] WuYinPlaylistModuleView.swift
- [ ] WuYinPlaylistsCarouselView.swift
- [ ] GongFaCoursesPageView.swift
- [ ] GongFaCourseDetailView.swift
- [ ] GongFaCoursesCarouselView.swift
- [ ] CasualListeningBoardView.swift
- [ ] GongFaVideoPlayerView.swift

#### 会员订阅
- [ ] MembershipCenterView.swift
- [ ] IAPService.swift（如有UI文本）

### 低优先级页面

#### 通知权限
- [ ] NotificationListView.swift
- [ ] NotificationRowView.swift

#### 登录启动
- [ ] LoginView.swift
- [ ] SplashView.swift

#### 其他视图
- [ ] HomeRedesignPlaceholderView.swift
- [ ] NewMainHomeView.swift
- [ ] VajraIconGridView.swift
- [ ] GreetingHeaderBar.swift
- [ ] UnifiedCommentListView.swift
- [ ] CommentDesignSystem.swift
- [ ] LocationSelectionView.swift
- [ ] PostPermissionSettingsView.swift
- [ ] TopicSelectionView.swift
- [ ] SelectablePersistenceDetailView.swift
- [ ] PersistenceDetailView.swift
- [ ] TwitterStyleImageGrid.swift
- [ ] ImageViewerSheet.swift
- [ ] VideoThumbnailView.swift
- [ ] CachedAsyncImage.swift
- [ ] EventSegmentsView.swift
- [ ] AppUsageAnalysisDetailView.swift

## 进度跟踪

您可以使用以下方法跟踪多语言适配进度：

### 1. 搜索硬编码文本

使用 grep 或 IDE 搜索功能查找硬编码中文：
```bash
grep -r 'Text("' --include="*.swift"
grep -r '.navigationTitle("' --include="*.swift"
grep -r 'Button("' --include="*.swift"
```

### 2. 检查文件

逐个检查文件，确保：
- ✅ 已导入 LocalizationManager
- ✅ 所有 Text() 使用本地化
- ✅ 所有 navigationTitle 使用本地化
- ✅ 所有 Button 文本使用本地化
- ✅ 所有 Alert 和 Sheet 标题使用本地化

### 3. 测试

切换语言后测试每个页面：
```swift
// 在设置页面添加语言切换功能
LocalizationManager.shared.changeLanguage(to: "en")
LocalizationManager.shared.changeLanguage(to: "ja")
LocalizationManager.shared.changeLanguage(to: "ko")
LocalizationManager.shared.changeLanguage(to: "zh-Hant")
```

## 常见问题

### Q: 如何处理带参数的文本？

A: 使用 String 插值：
```swift
let days = 7
let text = "\(days) \(localizationManager.localizedString(key: "days"))"
```

或者创建格式化方法：
```swift
func localizedDays(_ count: Int) -> String {
    "\(count) \(localizationManager.localizedString(key: "days"))"
}
```

### Q: 如何处理复数形式？

A: 创建特定的键：
```swift
case "one_day": return "1天"
case "days": return "天"

// 使用时：
let text = count == 1 
    ? localizationManager.localizedString(key: "one_day")
    : "\(count) \(localizationManager.localizedString(key: "days"))"
```

### Q: 如何处理很长的文本段落？

A: 仍然使用键值对，但可以在键名中标明是段落：
```swift
case "intro_paragraph": return "这是一段很长的介绍文字..."
case "terms_paragraph_1": return "第一段条款内容..."
```

### Q: AttributedString 如何处理？

A: 先本地化，再创建 AttributedString：
```swift
let localizedText = localizationManager.localizedString(key: "formatted_text")
let attributed = AttributedString(localizedText)
```

## 示例：完整页面适配

以下是一个完整的页面适配示例：

### 原始代码（SettingsView.swift）

```swift
struct SettingsView: View {
    var body: some View {
        NavigationView {
            List {
                Section(header: Text("账号")) {
                    NavigationLink("个人信息") {
                        ProfileEditView()
                    }
                    NavigationLink("安全设置") {
                        SecurityView()
                    }
                }
                
                Section(header: Text("通用")) {
                    Toggle("深色模式", isOn: $darkMode)
                    Picker("语言", selection: $language) {
                        Text("简体中文").tag("zh")
                        Text("English").tag("en")
                    }
                }
                
                Section {
                    Button("退出登录") {
                        logout()
                    }
                    .foregroundColor(.red)
                }
            }
            .navigationTitle("设置")
        }
    }
}
```

### 适配后代码

```swift
struct SettingsView: View {
    @StateObject private var localizationManager = LocalizationManager.shared
    @State private var darkMode = false
    @State private var language = "zh"
    
    var body: some View {
        NavigationView {
            List {
                Section(header: Text(localizationManager.localizedString(key: "account"))) {
                    NavigationLink(localizationManager.localizedString(key: "profile")) {
                        ProfileEditView()
                    }
                    NavigationLink(localizationManager.localizedString(key: "account_security"))) {
                        SecurityView()
                    }
                }
                
                Section(header: Text(localizationManager.localizedString(key: "general"))) {
                    Toggle(localizationManager.localizedString(key: "dark_mode"), isOn: $darkMode)
                    Picker(localizationManager.localizedString(key: "language"), selection: $language) {
                        Text(localizationManager.localizedString(key: "simplified_chinese")).tag("zh")
                        Text(localizationManager.localizedString(key: "english")).tag("en")
                    }
                }
                
                Section {
                    Button(localizationManager.localizedString(key: "logout")) {
                        logout()
                    }
                    .foregroundColor(.red)
                }
            }
            .navigationTitle(localizationManager.localizedString(key: "settings"))
        }
    }
}
```

### 需要添加的文本键

```swift
// 在 LocalizedStrings 中添加
case "account": return "账号"
case "general": return "通用"
case "simplified_chinese": return "简体中文"
case "english": return "English"
// 其他键已在现有库中
```

## 贡献指南

如果您正在为多语言适配做贡献：

1. **一次处理一个页面**：完整适配一个页面后再移动到下一个
2. **测试每个更改**：确保文本正确显示
3. **提交清晰的注释**：说明哪些页面已适配
4. **更新此文档**：在待完成清单中标记已完成的页面
5. **添加新键时保持一致**：遵循现有的命名和组织规范

## 总结

多语言适配是一个系统性工程，需要：
1. ✅ 核心基础设施（已完成）
2. ✅ 全面的文本键库（已完成 400+ 键）
3. ✅ 示例页面（已完成 3-4 个页面）
4. 🔄 逐页适配（进行中，需要适配 100+ 页面）
5. 🔄 测试和优化（持续进行）

按照本指南，您可以系统地为所有页面添加多语言支持。祝您适配顺利！

---

**最后更新**：2025-10-09  
**版本**：1.0  
**维护者**：青禾开发团队


# 诊断连续包月购买问题

## 🐛 问题描述

**现象**：
- 点击"连续包月"套餐 → 弹出 Apple ID 登录提示
- 点击"取消登录" → 显示"购买成功"
- 其他套餐（月度、季度、年度）→ 正常显示沙箱支付

## ✅ 问题已找到！

### 根本原因：App Store Connect 中的产品价格配置错误

从日志中发现：
```
✅ 找到产品: com.qinghe.qinghe.membership.monthlyv4 - 连续包月
💰 价格: ¥198.00  ← ❌ 错误！应该是 ¥29.9
```

**分析**：
1. ✅ 产品加载成功（4个产品都正常）
2. ✅ 产品映射正确（planCode 匹配）
3. ❌ **App Store Connect 中的价格是 ¥198.00，与预期的 ¥29.9 不符**

**为什么会要求登录 Apple ID？**
- 真机环境下，StoreKit 会从 App Store Connect 获取产品信息
- 当发现价格异常时，会尝试重新验证产品信息
- 这时就会要求登录 Apple ID

## 🔍 原可能的原因（已排除）

### 原因 1：StoreKit 产品未正确加载

当 StoreKit 找不到产品时，会要求登录 Apple ID 去 App Store Connect 查找产品。

**检查方法**：查看控制台日志

### 原因 2：产品 ID 映射错误

后端返回的 `planCode` 与 StoreKit 产品 ID 的映射可能有问题。

### 原因 3：产品加载顺序问题

第一个产品可能在 StoreKit 初始化时还没准备好。

## 🧪 诊断步骤

### 步骤 1：查看产品加载日志

1. 运行应用
2. 进入会员购买页面
3. 查看 Xcode 控制台日志，找到以下关键信息：

```
📦 开始加载产品列表...
✅ 后端返回 X 个产品
  - com.qinghe.qinghe.membership.monthlyv4 -> planCode: monthly_auto
  - com.qinghe.qinghe.membership.monthly.autov5 -> planCode: ???
  - com.qinghe.qinghe.membership.monthly.autov6 -> planCode: ???
  - com.qinghe.qinghe.membership.monthly.autov7 -> planCode: ???
🔍 从 StoreKit 请求 X 个产品: [...]
✅ StoreKit 返回 X 个产品
  - com.qinghe.qinghe.membership.monthlyv4: 连续包月会员 - ¥29.9
  - ...
```

**关键检查点**：
- ✅ 后端返回的产品数量应该是 4
- ✅ StoreKit 返回的产品数量也应该是 4
- ✅ 每个产品的 planCode 应该正确映射

### 步骤 2：查看购买流程日志

1. 点击"连续包月"套餐
2. 查看控制台日志：

```
🔍 查找产品 planCode: monthly_auto
🔍 后端产品数量: 4
🔍 StoreKit 产品数量: 4
  - 后端产品: com.qinghe.qinghe.membership.monthlyv4, planCode: monthly_auto
  - 后端产品: com.qinghe.qinghe.membership.monthly.autov5, planCode: ???
  - ...
🔍 StoreKit 已加载的产品:
  - StoreKit产品: com.qinghe.qinghe.membership.monthlyv4 -> 连续包月会员
  - StoreKit产品: com.qinghe.qinghe.membership.monthly.autov5 -> 月度会员
  - ...
✅ 找到匹配的后端产品: com.qinghe.qinghe.membership.monthlyv4
✅ 找到对应的 StoreKit 产品: 连续包月会员
```

**如果看到以下错误**：
```
❌ 未找到对应的 StoreKit 产品
❌ 可能原因: StoreKit 配置文件中缺少产品 com.qinghe.qinghe.membership.monthlyv4
```

**说明**：StoreKit 配置文件有问题。

### 步骤 3：检查 StoreKit 配置文件

打开 `qinghe/ /qinghe/qinghe/Configuration.storekit`，确认：

1. **产品 ID 是否正确**：
   ```json
   "productID" : "com.qinghe.qinghe.membership.monthlyv4"
   ```

2. **产品是否在正确的数组中**：
   - 应该在 `nonRenewingSubscriptions` 数组中
   - 不应该在 `subscriptions` 数组中（除非是自动续期订阅）

3. **产品配置是否完整**：
   ```json
   {
     "displayPrice" : "29.9",
     "familyShareable" : false,
     "internalID" : "6670002001",
     "localizations" : [...],
     "productID" : "com.qinghe.qinghe.membership.monthlyv4",
     "price" : 29.9,
     "currency" : "CNY",
     "referenceName" : "连续包月会员",
     "type" : "NonRenewingSubscription"
   }
   ```

### 步骤 4：检查 Xcode Scheme 配置

1. 在 Xcode 中，点击顶部的 Scheme 选择器（qinghe）
2. 选择 "Edit Scheme..."
3. 选择左侧的 "Run"
4. 切换到 "Options" 标签
5. 确认 **StoreKit Configuration** 设置为 `Configuration.storekit`

**如果没有设置**：
- 点击下拉菜单
- 选择 `Configuration.storekit`
- 点击 "Close"
- 重新运行应用

### 步骤 5：重置模拟器

如果以上步骤都正常，尝试重置模拟器：

```bash
# 列出所有模拟器
xcrun simctl list devices

# 重置特定模拟器（替换 DEVICE_ID）
xcrun simctl erase <DEVICE_ID>

# 或者重置所有模拟器
xcrun simctl erase all
```

## 🔧 解决方案

### ⭐ 方案 1：修改 App Store Connect 中的产品价格（推荐）

**步骤**：

1. **登录 App Store Connect**
   - 访问 https://appstoreconnect.apple.com
   - 使用您的开发者账号登录

2. **找到产品配置**
   - 选择您的应用（青禾计划）
   - 点击左侧菜单的 **"App 内购买项目"**
   - 找到产品 `com.qinghe.qinghe.membership.monthlyv4`（连续包月会员）

3. **检查并修改价格**
   - 当前价格：**¥198.00** ❌
   - 应该修改为：**¥29.9** ✅
   - 或者根据您的定价策略选择合适的价格档位

4. **保存并等待同步**
   - 点击"保存"
   - 等待 App Store Connect 同步（通常需要几分钟到几小时）
   - 在沙盒环境中测试

5. **验证修改**
   - 重新运行应用
   - 查看控制台日志，确认价格已更新
   - 测试购买流程

**注意事项**：
- App Store Connect 的价格修改可能需要审核
- 如果产品已经上线，修改价格可能影响现有用户
- 建议在沙盒环境充分测试后再发布

---

### 方案 2：使用沙盒测试账号（临时解决）

如果您只是在测试阶段，可以：

1. **在设备上登录沙盒测试账号**
   - 设置 → App Store → 沙盒账户
   - 登录您的沙盒测试账号

2. **重新测试购买**
   - 沙盒环境会使用 StoreKit 配置文件的价格
   - 不会要求登录 Apple ID

---

### 方案 3：检查产品类型配置

从日志中看到，连续包月的产品类型可能配置不正确：

**后端返回**：
```json
"product_type": "auto_renewable_subscription"  // 自动续期订阅
```

**StoreKit 配置**：
```json
"type": "NonRenewingSubscription"  // 非自动续期订阅
```

**建议**：
- 如果是"连续包月"，应该使用 `AutoRenewableSubscription`（自动续期订阅）
- 如果是"单次购买"，应该使用 `NonRenewingSubscription`（非自动续期订阅）

**修改 StoreKit 配置文件**：

将 `Configuration.storekit` 中的连续包月产品从 `nonRenewingSubscriptions` 移到 `subscriptions` 数组：

```json
{
  "subscriptions": [
    {
      "displayPrice": "29.9",
      "familyShareable": false,
      "internalID": "6670002001",
      "localizations": [...],
      "productID": "com.qinghe.qinghe.membership.monthlyv4",
      "referenceName": "连续包月会员",
      "subscriptionDuration": "P1M",  // 1个月
      "type": "RecurringSubscription"
    }
  ],
  "nonRenewingSubscriptions": [
    // 其他非自动续期的产品
  ]
}
```

---

### 方案 4：临时禁用价格检查（不推荐）

如果您急需测试其他功能，可以临时在代码中添加价格覆盖：

```swift
// 在 MembershipCenterView 中显示价格时
var displayPrice: String {
    if plan.planCode == "monthly_auto" {
        return "¥29.9"  // 临时覆盖显示价格
    }
    return plan.price
}
```

**注意**：这只是临时方案，不解决根本问题。

---

## 🎯 推荐的解决步骤

### 第一步：立即修改 App Store Connect 价格

1. 登录 App Store Connect
2. 找到 `com.qinghe.qinghe.membership.monthlyv4` 产品
3. 将价格从 ¥198.00 改为 ¥29.9
4. 保存并等待同步

### 第二步：验证产品类型

1. 确认"连续包月"是否应该是自动续期订阅
2. 如果是，修改 StoreKit 配置文件和 App Store Connect 中的产品类型
3. 确保后端、StoreKit 配置、App Store Connect 三者一致

### 第三步：测试验证

1. 重新运行应用
2. 查看控制台日志，确认价格正确
3. 测试购买流程，确认不再要求登录 Apple ID
4. 测试取消购买，确认不显示"购买成功"

---

## 📊 预期的修复后日志

```
✅ 找到产品: com.qinghe.qinghe.membership.monthlyv4 - 连续包月
💰 价格: ¥29.90  ← ✅ 正确！
🔄 开始调用 StoreKit 购买...
[StoreKit 弹出沙盒支付对话框，不要求登录 Apple ID]
```

---

## 🔍 其他可能的解决方案（已排除，但保留供参考）

### 解决方案 X：确保 StoreKit 配置文件正确加载

在 Xcode 中：
1. Product → Scheme → Edit Scheme...
2. Run → Options
3. StoreKit Configuration → 选择 `Configuration.storekit`
4. 重新运行应用

### 解决方案 2：检查后端返回的 planCode

确认后端 API `/apple-iap/products` 返回的数据中，`monthly_auto` 套餐的配置正确：

```json
{
  "product_id": "com.qinghe.qinghe.membership.monthlyv4",
  "membership_plan": {
    "plan_code": "monthly_auto",
    "plan_name": "连续包月会员"
  }
}
```

### 解决方案 3：添加产品加载重试机制

如果产品加载失败，可以添加重试逻辑：

```swift
// 在 MembershipCenterView 的 onAppear 中
.onAppear {
    Task {
        await viewModel.load()
        
        // 如果产品列表为空，重试加载
        if viewModel.plans.isEmpty {
            print("⚠️ 产品列表为空，1秒后重试...")
            try? await Task.sleep(nanoseconds: 1_000_000_000)
            await viewModel.load()
        }
    }
}
```

### 解决方案 4：修复"取消登录显示购买成功"的问题

这个问题已经在之前的修复中解决了（添加 `purchaseSuccess` 标志）。

但是，如果用户点击"取消登录"，说明 StoreKit 根本没有找到产品，所以不会进入购买流程。

**需要额外处理**：在 `IAPService.purchase()` 方法中，如果找不到产品，应该抛出错误：

```swift
guard let product = product(for: planCode) else {
    print("❌ 未找到匹配的内购产品: \(planCode)")
    throw IAPError.productNotFound  // ✅ 确保抛出错误
}
```

## 📊 预期的正常日志

### 产品加载成功
```
📦 开始加载产品列表...
✅ 后端返回 4 个产品
  - com.qinghe.qinghe.membership.monthlyv4 -> planCode: monthly_auto
  - com.qinghe.qinghe.membership.monthly.autov5 -> planCode: monthly
  - com.qinghe.qinghe.membership.monthly.autov6 -> planCode: quarterly
  - com.qinghe.qinghe.membership.monthly.autov7 -> planCode: yearly
🔍 从 StoreKit 请求 4 个产品: [...]
🖥️ 运行环境: 模拟器
✅ StoreKit 返回 4 个产品
  - com.qinghe.qinghe.membership.monthlyv4: 连续包月会员 - ¥29.9
  - com.qinghe.qinghe.membership.monthly.autov5: 月度会员 - ¥39.9
  - com.qinghe.qinghe.membership.monthly.autov6: 季度会员 - ¥69.9
  - com.qinghe.qinghe.membership.monthly.autov7: 年度会员 - ¥169
```

### 购买流程成功
```
🛒 开始购买流程...
📦 计划代码: monthly_auto
🔍 查找产品 planCode: monthly_auto
🔍 后端产品数量: 4
🔍 StoreKit 产品数量: 4
✅ 找到匹配的后端产品: com.qinghe.qinghe.membership.monthlyv4
✅ 找到对应的 StoreKit 产品: 连续包月会员
✅ 找到产品: com.qinghe.qinghe.membership.monthlyv4 - 连续包月会员
💰 价格: ¥29.9
🔄 开始调用 StoreKit 购买...
[StoreKit 弹出支付对话框]
```

## 🎯 下一步行动

1. **运行应用并查看日志**
2. **找到问题所在**（产品未加载 / 产品 ID 不匹配 / StoreKit 配置未启用）
3. **应用对应的解决方案**
4. **重新测试购买流程**

## 📝 需要提供的信息

如果问题仍然存在，请提供以下信息：

1. **完整的控制台日志**（从应用启动到点击购买）
2. **后端 API 返回的产品列表**（`/apple-iap/products` 的响应）
3. **Xcode Scheme 中的 StoreKit Configuration 设置**（截图）

---

**更新日期**：2025-10-13  
**相关文件**：
- `qinghe/ /qinghe/qinghe/IAPService.swift`
- `qinghe/ /qinghe/qinghe/Configuration.storekit`
- `qinghe/ /qinghe/qinghe/MembershipCenterView.swift`
